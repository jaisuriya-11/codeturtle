FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY shared ./shared
COPY worker ./worker
COPY ingest ./ingest
ENV PORT=8080 QUEUE_BACKEND=cloudtasks
CMD ["sh", "-c", "uvicorn ingest.main:app --host 0.0.0.0 --port ${PORT}"]