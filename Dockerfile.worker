FROM python:3.12-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY shared ./shared
COPY worker ./worker
COPY defaults ./defaults
ENV PORT=8080
CMD ["sh", "-c", "uvicorn worker.main:app --host 0.0.0.0 --port ${PORT} --timeout-keep-alive 600"]